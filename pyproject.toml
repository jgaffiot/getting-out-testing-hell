[project]
name = "bookstore"
version = "0.1.0"
description = "A bookstore API — workshop starting point"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy>=2.0",
    "psycopg2-binary>=2.9",
    "alembic>=1.13",
    "pydantic[email]>=2.7",
    "pydantic-settings>=2.3",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.14",
    "ty>=0.0.38",
]

[project.optional-dependencies]
test = [
    "pytest>=8.2",
    "pytest-cov>=5.0",
    "requests>=2.32",
]
# Self-contained extra for running the reference solution under solution/
solution = [
    "pytest>=8.2",
    "pytest-cov>=5.0",
    "testcontainers[postgres]>=4.8",
    "respx>=0.21",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["app"]
